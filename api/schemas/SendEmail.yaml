description: Model for sending email
properties:
  email:
    type: string
    pattern: ^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$
    x-pattern-error: Nieprawidłowy adres email
required:
  - email
